apiVersion: v1
kind: Pod
metadata:
  name: my-nginx-pod
  labels:
    app: nginx
spec:
  containers:
  - name: nginx
    image: nginx:latest         # Use the latest NGINX image from Docker Hub
    ports:
    - containerPort: 8080        # Expose port 80 for HTTP traffic
